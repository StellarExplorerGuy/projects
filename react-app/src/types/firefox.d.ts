declare namespace browser {
    // Define the structure of the browser namespace here
    // Example:
    namespace runtime {
      function getURL(path: string): string;
      // Add other methods or properties you use from the browser runtime API
    }
    // Add declarations for other browser API namespaces if needed
  }